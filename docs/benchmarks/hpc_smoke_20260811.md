# HPC GPU and environment smoke results

Date: 2026-08-11. Both jobs used `HPC-RTX3070-08`, one RTX3070, two CPUs, 8 GB
RAM, and a ten-minute limit. Nodes 06 and 07 were excluded. These are
infrastructure gates, not throughput or playing-strength benchmarks.

| Job | Environment | Elapsed | Slurm result | Checks |
| ---: | --- | ---: | --- | --- |
| 33475 | Existing Python 3.9 / TensorFlow 2.14 | 31 s | `COMPLETED 0:0` | GPU placement, two-head train, save/load |
| 33476 | Project Python 3.11 / TensorFlow 2.21 | 38 s | `COMPLETED 0:0` | `pip check`, 46 tests, GPU placement, train, save/load |

The project-environment Keras smoke reported:

```json
{
  "cuda_build": true,
  "elapsed_seconds": 17.923,
  "gpu": "/physical_device:GPU:0",
  "loss": 6.630066871643066,
  "matmul_device": "/job:localhost/replica:0/task:0/device:GPU:0",
  "max_weight_change": 0.0009999871253967285,
  "status": "pass",
  "tensorflow": "2.21.0"
}
```

The driver reported CUDA 12.4, the pip runtime 12.9, the TensorFlow toolkit
12.5, and cuDNN 9.24. NVIDIA CUDA 12 minor-version compatibility was therefore
exercised rather than merely inferred. No checkpoint was retained.
